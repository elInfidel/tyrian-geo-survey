import IApplicationState from './Interfaces';

const initialState: IApplicationState = {
    continents = [],
}

export function applicationReducers(state: IApplicationState = initialState, action: any): IApplicationState {

}